export enum SLICE_NAME {
  APP = "app",
  USER = "user"
}

export enum ACTION_NAME {
  GET = "get",
  UPDATE = "update",
  DELETE = "delete",
  ADD = "add"
}
export enum LocalStorageKey {
  ROOT = 'root'
}
